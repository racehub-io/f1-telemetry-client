export interface ITeam {
  name: string;
  color: string;
}

export interface IDriver {
  firstName: string;
  lastName: string;
  abbreviation: string;
}

export interface ITrack {
  name: string;
}
